## Group colors for {{ study_name }}

# Edit the code below with group names and HEX colors.
# The standard colors used at VISC are [edit].
# The control group is usually designated with black (HEX # [edit]).
# Non-response is usually designated with grey (HEX # [edit]).

group_colors <- c(
  `Group 1` = "",
  `Group 2` = "",
  `Group 3` = "",
  `Group 4` = ""
)

# To source these group colors in your Rmarkdown report:
#  knitr::read_chunk("protocol/group-colors.R")

# In ggplot2, you can set colors manually with:
#  scale_color_manual(values = group_colors)
